{
  "name": "wave-mcp-proxy",
  "version": "2.0.0",
  "description": "Thin local proxy for Wave OS MCP — zero secrets, just forwards to Base44 backend",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "postinstall": "echo '✅ Wave MCP Proxy installed. No build needed — just configure .cursor/mcp.json'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
